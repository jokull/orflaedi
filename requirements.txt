# Direct dependencies of the orflaedi scraper.
#
# Installed in the scraper image (Dockerfile.scrapy) and the Scrapy Cloud
# image (scrapinghub.yml). Transitive dependencies are resolved by pip.
#
# The legacy FastAPI/starlette web stack (fastapi, starlette, uvicorn,
# pydantic, jinja2, ...) was removed from this file — the web frontend is now
# the static Astro build in web/ deployed to Cloudflare Workers.

scrapy==2.16.0
Twisted==22.10.0
shub==2.16.0
SQLAlchemy==1.4.32
psycopg2==2.9.3
